---
phase: 02-device-discovery-heartbeat-audit
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - web_panel/server.py
autonomous: true
requirements:
  - MGMT-03
user_setup: []
must_haves:
  truths:
    - Devices list can be filtered by hostname, username, and OS
    - Search query parameter is supported in backend and UI
  artifacts:
    - web_panel/server.py
  key_links:
    - get_devices_list supports search_query filter in server.py
---

<objective>
Implement search and filtering by hostname, username, and OS in both the UI devices view and backend database queries.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-device-discovery-heartbeat-audit/02-CONTEXT.md
@web_panel/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add backend filtering to get_devices_list and web routes</name>
  <files>web_panel/server.py</files>
  <action>Update `get_devices_list` function to accept an optional `search_query` argument and apply a SQL `LIKE` filter on the `hostname`, `username`, and `os` columns. Update the `/devices` and `/api/admin/devices` endpoints to read the `search` query parameter and pass it to the query.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import get_devices_list; print('Backend search parameter verified')"</verify>
  <acceptance_criteria>
    - `get_devices_list` accepts `search_query` and does not raise syntax/type errors.
  </acceptance_criteria>
  <done>Backend filtering is integrated into the devices query and API endpoints.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate search in the UI templates</name>
  <files>web_panel/server.py</files>
  <action>Update `DEVICES_HTML` to pass any initial search query to the DataTables initialization so that client-side filtering aligns with the initial backend query.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import DEVICES_HTML; print('DEVICES_HTML loaded')"</verify>
  <acceptance_criteria>
    - DEVICES_HTML loads without Python template syntax errors.
  </acceptance_criteria>
  <done>UI devices list is configured to support pre-filtered search terms.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Run the server and verify `/devices?search=test` filters the output and loads successfully.
</verification>

<success_criteria>
- Backend database query supports filtering by hostname, username, and OS.
- Frontend matches the search query.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-discovery-heartbeat-audit/02-02-SUMMARY.md`
</output>
