---
phase: 02-device-discovery-heartbeat-audit
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web_panel/server.py
autonomous: true
requirements:
  - MGMT-01
  - MGMT-02
user_setup: []
must_haves:
  truths:
    - Devices whose last heartbeat was within the last 30 seconds show as Online
    - All read paths update offline devices to status 0 if they haven't checked in within 30 seconds
  artifacts:
    - web_panel/server.py
  key_links:
    - update_offline_devices helper is defined and used in server.py
---

<objective>
Audit and fix `/api/heartbeat` logic and SQLite schema triggers to ensure all online client devices are reliably registered and their online/offline state is updated in real-time based on a 30-second window.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-device-discovery-heartbeat-audit/02-CONTEXT.md
@web_panel/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add update_offline_devices helper</name>
  <files>web_panel/server.py</files>
  <action>Define a helper function `update_offline_devices(conn)` in `server.py` that executes an UPDATE statement to set `online = 0` for any devices where `last_seen < datetime('now', '-30 seconds')` or `last_seen IS NULL`.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import update_offline_devices; print('Helper loaded')"</verify>
  <acceptance_criteria>
    - `update_offline_devices` function is successfully imported and executes without syntax errors.
  </acceptance_criteria>
  <done>`update_offline_devices` helper is defined.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate offline update in read paths and audit write paths</name>
  <files>web_panel/server.py</files>
  <action>Integrate `update_offline_devices(conn)` in `get_devices_list`, `web_dashboard`, `api_admin_devices`, and `/api/devices`. Update `/api/heartbeat` to use `-30 seconds` instead of `-60 seconds` for the offline cutoff query.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import get_devices_list; print('Paths verified')"</verify>
  <acceptance_criteria>
    - No syntax errors when importing `get_devices_list`.
  </acceptance_criteria>
  <done>Read and write paths updated for the 30-second online status window.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- Verify server compiles and starts without database lock or query errors.
- Test a mocked heartbeat and verify device gets inserted and marked online, then marked offline after 30 seconds.
</verification>

<success_criteria>
- Heartbeat endpoint uses 30-second window.
- Device online status is refreshed on dashboard and devices listing requests.
</success_criteria>

<output>
After completion, create `.planning/phases/02-device-discovery-heartbeat-audit/02-01-SUMMARY.md`
</output>
