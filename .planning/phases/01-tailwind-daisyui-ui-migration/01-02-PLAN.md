---
phase: 01-tailwind-daisyui-ui-migration
plan: 02
type: execute
wave: 2
depends_on:
  - "01-01"
files_modified:
  - web_panel/server.py
autonomous: true
requirements:
  - UI-02
  - UI-03
user_setup: []
must_haves:
  truths:
    - Dashboard renders stats using DaisyUI stats components
    - Devices page displays devices in a DaisyUI table with connection actions
  artifacts:
    - web_panel/server.py
  key_links:
    - DASHBOARD_HTML and DEVICES_HTML are defined and parsed in server.py
---

<objective>
Migrate the Dashboard and Devices HTML views to use DaisyUI styling tokens and widgets.

Purpose: Upgrades the primary operational admin views to modern, cohesive visual designs.
Output: Refactored DASHBOARD_HTML and DEVICES_HTML variables in server.py.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-tailwind-daisyui-ui-migration/01-01-SUMMARY.md
@web_panel/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migrate Dashboard View (DASHBOARD_HTML)</name>
  <files>web_panel/server.py</files>
  <action>Update DASHBOARD_HTML in server.py. Replace the old stats section with a responsive grid of DaisyUI 'stats' and 'stat' cards. Place charts inside card-custom and card-body. Style the recent devices preview table with DaisyUI table styles.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import DASHBOARD_HTML; print('DASHBOARD_HTML loaded')"</verify>
  <acceptance_criteria>
    - DASHBOARD_HTML parses without Python syntax errors
  </acceptance_criteria>
  <done>DASHBOARD_HTML contains DaisyUI components</done>
</task>

<task type="auto">
  <name>Task 2: Migrate Devices View (DEVICES_HTML)</name>
  <files>web_panel/server.py</files>
  <action>Update DEVICES_HTML in server.py. Design the main table using DaisyUI table classes. Set up status badges as badge-success (online) and badge-ghost (offline) with a pulsing green indicator. Redesign the details modal structure with DaisyUI modal elements.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import DEVICES_HTML; print('DEVICES_HTML loaded')"</verify>
  <acceptance_criteria>
    - DEVICES_HTML parses without Python syntax errors
  </acceptance_criteria>
  <done>DEVICES_HTML contains DaisyUI components</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify server compiles and starts without template rendering errors
- [ ] Inspect DASHBOARD_HTML and DEVICES_HTML strings to confirm old bootstrap/custom styling is replaced by DaisyUI classes
</verification>

<success_criteria>
- All tasks completed
- Dashboard HTML uses DaisyUI stats and layout components
- Devices HTML uses DaisyUI table and modal components
</success_criteria>

<output>
After completion, create `.planning/phases/01-tailwind-daisyui-ui-migration/01-02-SUMMARY.md`
</output>
