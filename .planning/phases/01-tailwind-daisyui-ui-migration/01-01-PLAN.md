---
phase: 01-tailwind-daisyui-ui-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - web_panel/package.json
  - web_panel/tailwind.config.js
  - web_panel/src/input.css
  - web_panel/server.py
autonomous: true
requirements:
  - UI-01
  - UI-06
user_setup: []
must_haves:
  truths:
    - Tailwind compilation compiles input.css into output.css without errors
    - The base HTML contains DaisyUI theme and layout elements
  artifacts:
    - web_panel/static/output.css
  key_links:
    - web_panel/server.py links to /static/output.css
---

<objective>
Configure the Tailwind and DaisyUI build environment in the web panel workspace, refactor input styling, and update the base HTML layout to use DaisyUI drawer and header structures.

Purpose: Establishes the visual and compiler foundation for all subsequent UI page migrations.
Output: Configured tailwind.config.js, package.json dependencies, refactored input.css, and DaisyUI integrated BASE_HTML.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@web_panel/package.json
@web_panel/tailwind.config.js
@web_panel/src/input.css
@web_panel/server.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install dev dependencies in package.json</name>
  <files>web_panel/package.json</files>
  <action>Verify and install dependencies in package.json. Ensure daisyui is in devDependencies. Run npm install inside web_panel/ directory.</action>
  <verify>npm list --prefix web_panel daisyui</verify>
  <acceptance_criteria>
    - Command output shows daisyui is installed
  </acceptance_criteria>
  <done>daisyui npm package is installed and present in node_modules</done>
</task>

<task type="auto">
  <name>Task 2: Configure tailwind.config.js</name>
  <files>web_panel/tailwind.config.js</files>
  <action>Ensure tailwind.config.js imports the daisyui plugin using require("daisyui"). Set up content paths to match server.py and templates.</action>
  <verify>grep -q "daisyui" web_panel/tailwind.config.js</verify>
  <acceptance_criteria>
    - tailwind.config.js contains daisyui in plugins array
  </acceptance_criteria>
  <done>tailwind.config.js is configured with daisyui</done>
</task>

<task type="auto">
  <name>Task 3: Refactor input.css and setup base classes</name>
  <files>web_panel/src/input.css</files>
  <action>Ensure input.css imports Tailwind base directives: @tailwind base, @tailwind components, @tailwind utilities. Define custom theme overrides and custom classes if needed using Tailwind @apply directives.</action>
  <verify>grep -q "@tailwind base" web_panel/src/input.css</verify>
  <acceptance_criteria>
    - input.css contains standard Tailwind base, components, and utilities imports
  </acceptance_criteria>
  <done>input.css contains tailwind directives</done>
</task>

<task type="auto">
  <name>Task 4: Integrate DaisyUI with BASE_HTML in server.py</name>
  <files>web_panel/server.py</files>
  <action>Edit BASE_HTML string variable in server.py to implement DaisyUI base components (such as sidebar drawer, top navbar, theme toggler, and content frame). Map the active page highlighting using DaisyUI list styling.</action>
  <verify>python -c "import sys; sys.path.append('web_panel'); from server import BASE_HTML; print('BASE_HTML loaded')"</verify>
  <acceptance_criteria>
    - python loading BASE_HTML is successful without syntax errors
  </acceptance_criteria>
  <done>BASE_HTML contains DaisyUI components</done>
</task>

<task type="auto">
  <name>Task 5: Compile Tailwind stylesheet</name>
  <files>web_panel/static/output.css</files>
  <action>Run the Tailwind CSS build script to compile input.css into output.css. Execute npm run build inside web_panel/.</action>
  <verify>test -f web_panel/static/output.css</verify>
  <acceptance_criteria>
    - output.css file exists and is compiled
  </acceptance_criteria>
  <done>Tailwind output.css builds successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify tailwind compiles cleanly via npm run build
- [ ] Verify BASE_HTML contains links to static/output.css and valid DaisyUI components
</verification>

<success_criteria>
- All tasks completed
- Tailwind output.css builds successfully
- Base HTML is successfully updated to DaisyUI layout
</success_criteria>

<output>
After completion, create `.planning/phases/01-tailwind-daisyui-ui-migration/01-01-SUMMARY.md`
</output>
