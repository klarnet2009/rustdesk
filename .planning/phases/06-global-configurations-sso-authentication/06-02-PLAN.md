---
phase: 06-global-configurations-sso-authentication
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - rustdesk-server/web_panel/server.py
  - rustdesk/flutter/lib/models/user_model.dart
  - rustdesk/src/flutter_ffi.rs
autonomous: true
requirements:
  - SSO-01
  - SSO-02
user_setup: []
must_haves:
  truths:
    - Server `/api/login-sso` endpoint issues Negotiate challenge when unauthorized.
    - Client fetches Kerberos token on Windows and logs in passwordlessly.
  artifacts:
    - docs/architecture/integrations.md
    - docs/architecture/security.md
  key_links: []
---

<objective>
Implement Active Directory Kerberos SSO authentication, covering client-side Windows SSPI token acquisition, server-side Negotiate validation (using pyspnego), and parallel login fallback mapping.

Purpose: Automates desktop client login for enterprise domain users.
Output: Integrated SSPI token generation in Rust FFI, server-side Negotiate REST API, and fallback login flow in Flutter.
</objective>

<execution_context>
@~/.gemini/antigravity/gsd-core/workflows/execute-plan.md
@~/.gemini/antigravity/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@rustdesk-server/web_panel/server.py
@rustdesk/flutter/lib/models/user_model.dart
@rustdesk/src/flutter_ffi.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement Server Negotiate SSO Endpoint</name>
  <files>rustdesk-server/web_panel/server.py</files>
  <action>Add /api/login-sso endpoint to server.py. Integrate Negotiate challenge response (401 with WWW-Authenticate: Negotiate) and pyspnego-based verification. If verification succeeds, map user principal name (UPN) to user database role and return JWT token.</action>
  <verify>python -c "import sys; sys.path.append('rustdesk-server/web_panel'); from server import api_login_sso; print('login-sso route loaded')"</verify>
  <acceptance_criteria>
    - Python compiles server.py without syntax errors
  </acceptance_criteria>
  <done>Server SSO route is implemented</done>
</task>

<task type="auto">
  <name>Task 2: Implement Client Rust SSPI FFI</name>
  <files>rustdesk/src/flutter_ffi.rs</files>
  <action>Define secur32.dll FFI call bindings in rustdesk/src/flutter_ffi.rs (or helper file) and implement FFI function main_get_sso_token(spn: String) -> String that returns the base64-encoded Negotiate token for target SPN on Windows. Return empty string if not on Windows or if error occurs.</action>
  <verify>grep -q "pub fn main_get_sso_token" rustdesk/src/flutter_ffi.rs</verify>
  <acceptance_criteria>
    - main_get_sso_token FFI function is exported in flutter_ffi.rs
  </acceptance_criteria>
  <done>Client SSPI token collector is implemented in Rust core</done>
</task>

<task type="auto">
  <name>Task 3: Integrate SSO Flow in client UserModel</name>
  <files>rustdesk/flutter/lib/models/user_model.dart</files>
  <action>Update user_model.dart to check if client runs on Windows and try SSO login on startup. If FFI mainGetSsoToken succeeds, call /api/login-sso with Negotiate token. If response has valid JWT, login user; otherwise fall back to credentials form.</action>
  <verify>grep -q "login-sso" rustdesk/flutter/lib/models/user_model.dart</verify>
  <acceptance_criteria>
    - login-sso request sequence is integrated in user_model.dart
  </acceptance_criteria>
  <done>SSO client login flow is integrated in Flutter</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Verify Flask server runs cleanly without syntax errors
- [ ] Verify FFI definitions are exported in flutter_ffi.rs
</verification>

<success_criteria>
- All tasks completed
- Flask server successfully runs with the SSO route
- Windows client FFI and Flutter models compile and run
</success_criteria>

<output>
After completion, create `.planning/phases/06-global-configurations-sso-authentication/06-02-SUMMARY.md`
</output>
